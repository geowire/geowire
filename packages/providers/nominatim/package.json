{
  "name": "@geowire/provider-nominatim",
  "version": "0.1.0",
  "description": "GeoWire Nominatim (OpenStreetMap) provider — zero-config default, no API key",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@geowire/schema": "workspace:*",
    "@geowire/provider-sdk": "workspace:*"
  },
  "devDependencies": {
    "@geowire/provider-testkit": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
