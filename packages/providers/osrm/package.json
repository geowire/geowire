{
  "name": "@geowirehq/provider-osrm",
  "version": "0.6.0",
  "description": "GeoWire OSRM provider — directions & distance matrix over OpenStreetMap, no API key",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@geowirehq/schema": "workspace:*",
    "@geowirehq/provider-sdk": "workspace:*"
  },
  "devDependencies": {
    "@geowirehq/provider-testkit": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
