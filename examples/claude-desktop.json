{
  "mcpServers": {
    "geowire": {
      "command": "npx",
      "args": ["-y", "@geowire/mcp"],
      "env": {
        "GOOGLE_MAPS_API_KEY": "optional — remove to run OpenStreetMap-only"
      }
    }
  }
}
